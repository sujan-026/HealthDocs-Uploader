rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for development
    // WARNING: These rules are open for development purposes only
    // In production, you should implement proper authentication and authorization
    
    match /{document=**} {
      allow read, write: if true;
    }
    
    // More secure rules for production (commented out):
    /*
    // Patients can only access their own records
    match /Patient/{patientId} {
      allow read, write: if request.auth != null && request.auth.uid == patientId;
    }
    
    // Medical records can be accessed by authenticated users
    match /MedicalRecords/{recordId} {
      allow read, write: if request.auth != null;
    }
    
    // Consultations can be accessed by authenticated users
    match /Consultation/{consultationId} {
      allow read, write: if request.auth != null;
    }
    
    // Doctors can access their own profiles
    match /doctor/{doctorId} {
      allow read, write: if request.auth != null;
    }
    
    // Document types can be read by anyone
    match /DocumentType/{docTypeId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    */
  }
}